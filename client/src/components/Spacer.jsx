// Just like noob developer there is problem in the navbar so i adjust the spacer to add the space
// between the navbar and the content
import React from 'react';

const Spacer = () => {
    return <div className="h-12 md:h-15" />; 
};

export default Spacer;
